# Subgraph endpoint URL (used by apps/web)
# Example: https://api.studio.thegraph.com/query/12345/lilnouns/version/latest
VITE_SUBGRAPH_URL="https://example.com/subgraph"

# Chain selection (choose one)
# Either set name:
# VITE_CHAIN_NAME=sepolia
# Or set numeric id:
# VITE_CHAIN_ID=11155111

# Optional: explicit RPC endpoint for the selected chain
# If not set, viem falls back to the chain's default/public RPC.
# Example:
# VITE_RPC_HTTP_URL="https://ethereum-sepolia-rpc.publicnode.com"

# WalletConnect (optional)
# Create a project at walletconnect.com and set the ID to enable WalletConnect
# If unset, the app runs without WalletConnect.
VITE_WC_PROJECT_ID=""

# Site metadata (used in index.html only; optional)
# Update for production deployments to improve previews and rich cards.
VITE_SITE_TITLE="Lil Nouns ENS"
VITE_SITE_DESCRIPTION="Claim subnames under lilnouns.eth"
VITE_SITE_URL="http://localhost:5173"
VITE_OG_IMAGE="/og.png"

# Farcaster Frame metadata (optional)
VITE_FC_IMAGE="/og.png"
VITE_FC_BUTTON="Open App"
VITE_FC_LAUNCH_URL="http://localhost:5173"
VITE_FC_SPLASH_ICON="/splash.png"
VITE_FC_SPLASH_BG="#f6e6ee"

# Base path for serving the app (must end with a slash)
# Example: "/subfolder/" when deploying under https://example.com/subfolder/
VITE_BASE_PATH="/"
