# Subgraph endpoint URL (used by apps/web)
# Example: https://api.studio.thegraph.com/query/12345/lilnouns/version/latest
VITE_SUBGRAPH_URL="https://example.com/subgraph"

# Chain selection (choose one)
# Either set name:
# VITE_CHAIN_NAME=sepolia
# Or set numeric id:
# VITE_CHAIN_ID=11155111

# Optional: explicit RPC endpoint for the selected chain
# If not set, defaults to the chain's public RPC from viem.
# Example (Sepolia): https://ethereum-sepolia-rpc.publicnode.com
VITE_RPC_HTTP_URL=""
